{
  "name": "ohu",
  "version": "0.1.0",
  "private": true,
  "description": "Ohu — agentic cooperative procurement on Casper",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
